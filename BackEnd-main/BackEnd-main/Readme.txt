1. Activate virtual env using CMD by Docai\Scripts\activate
2. set GOOGLE_APPLICATION_CREDENTIALS="key path"
3. although we have set the key path but as it is not accepting the key so I also manually add the key path